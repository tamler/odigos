name: Brave Search
id: brave
description: Adds web search capability using the Brave Search API
category: tools
requires:
  - label: Brave Search API key
    url: https://brave.com/search/api/
config_keys:
  - key: brave_api_key
    required: true
    description: Brave Search API subscription token
    type: secret
