agent:
  name: "Odigos"

database:
  path: "data/odigos.db"

openrouter:
  default_model: "arcee-ai/trinity-large-preview:free"
  fallback_model: "z-ai/glm-4.5-air:free"
  max_tokens: 16384
  temperature: 0.7

telegram:
  mode: "polling"
  webhook_url: ""

server:
  host: "0.0.0.0"
  port: 8080
